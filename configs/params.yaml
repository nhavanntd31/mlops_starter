project:
  name: house-price-prediction
  version: "0.1.0"
  random_seed: 42

data:
  raw_path: data/raw/houses.csv
  processed_dir: data/processed
  test_size: 0.2
  val_size: 0.1

training:
  n_estimators: 200
  max_depth: 5
  learning_rate: 0.1
  subsample: 0.8
